usage: lore.py [-h] [-l LIST] title

Get Message-IDs for patches on lore.kernel.org

positional arguments:
  title

optional arguments:
  -h, --help            show this help message and exit
  -l LIST, --list LIST  mailinglist, default: linux-btrfs
